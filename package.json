{
  "name": "mathom-project-website",
  "private": true,
  "version": "1.0.0",
  "description": "Build and validation commands for the Mathom project website.",
  "type": "module",
  "scripts": {
    "build:website": "node scripts/build-website.mjs",
    "check:website": "node scripts/build-website.mjs --check"
  },
  "engines": {
    "node": ">=20"
  }
}
